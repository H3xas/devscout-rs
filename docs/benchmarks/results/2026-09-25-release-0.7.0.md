# Results — devscout 0.7.0 release run, 2026-09-25

> **Single run, no repeats.** One measurement of the final 0.7.0 source tree, in two parts: the
> resolver-precision audit, by the method of Runs 0-5 in
> [`2026-09-resolver-precision.md`](2026-09-resolver-precision.md), and the scripted eight-task
> scorecard of [`2026-08.md`](2026-08.md). Run 5 in the resolver-precision document measured the
> precise-tier refusals before the member-qualifier arity change landed, so it does not describe
> the resolver 0.7.0 ships; this document does. Every prediction below was committed before any
> figure in this document was measured. Neither earlier document is edited.

## Environment

Filled in with the results. Planned values:

```
Date            2026-09-25
Corpus          MassTransit/MassTransit @ 855cf1752c94ca9498e0c45ce8d09fdc9e957dd6 (bench/corpus.lock, registered)
Tool version    devscout 0.7.0, built from a `git archive` export of the release commit
Build           cargo build --release --locked -j 4, rustc 1.97.1 (8bab26f4f 2026-07-14), aarch64-apple-darwin
Oracle          tools/scout-semantic from the same export, SDK 9.0.305 (the tree's default host)
Host            macOS (Darwin 25.2.0), Apple M2 Max, arm64; 12 cores, 64 GiB RAM, otherwise idle
Baseline        ripgrep 15.2.0 (+PCRE2 10.45); peer: ast-grep 0.45.1 (both the versions 2026-08 ran)
Bench root      bench/ (throwaway; nothing installed globally)
Isolation       HOME, TMPDIR, CARGO_TARGET_DIR, SCOUT_REGISTRY and SCOUT_CONTENT_DB redirected
                under the bench root
Network         none: every step offline, the corpus restore included
Reps            audit 1; scorecard cold 1 + warm median of 5 per cell, as 2026-08
```

## Corpus pin and setup

| Language | Repository | Pin | License | Status |
| --- | --- | --- | --- | --- |
| C# | MassTransit/MassTransit | `855cf1752c94ca9498e0c45ce8d09fdc9e957dd6` | Apache-2.0 | registered — measured |

The corpus is a `git archive` export of the pinned commit, given a git directory whose `HEAD` is
that commit so the tree reads as a clean checkout of the pin, exactly what
[`bench/clone-corpus.sh`](../../../bench/clone-corpus.sh) produces. Each arm gets a fresh copy,
none carries a `.git/scout` directory, and no existing checkout is mapped.

```sh
git archive <release commit> | tar -x -C "$EXPORT"
CARGO_TARGET_DIR="$TARGET" cargo build --release --locked -j 4 --offline      # in $EXPORT
dotnet build tools/scout-semantic -c Release                                  # in $EXPORT
dotnet restore "$ORACLE_CORPUS/MassTransit.sln" -p:TargetFrameworks=net9.0
dotnet run --project tools/scout-semantic -c Release -- "$ORACLE_CORPUS/MassTransit.sln" \
  --root "$ORACLE_CORPUS" --out "$ORACLE/refs.jsonl" --units "$ORACLE/units.jsonl" \
  -p:TargetFrameworks=net9.0
cd "$CORPUS" && "$TARGET/release/devscout" map .
"$TARGET/release/devscout" audit --semantic "$ORACLE/refs.jsonl" --units "$ORACLE/units.jsonl"
"$TARGET/release/devscout" audit --semantic "$ORACLE/refs.jsonl" --units "$ORACLE/units.jsonl" --json
```

The scorecard runs from the bench root with the corpus at `bench/corpora/csharp`, so every
`rg` and `ast-grep` path carries the same prefix, and the same payload bytes, as in 2026-08.

## What this measures / does not measure

- **Resolver precision**: per-edge precision by tier, recall over in-tree member sites and by
  receiver kind, the external-receiver leak, fan-out, and the top-20 false-positive and
  missed-target tables, with the metric definitions, join rule and exclusions of
  [`methodology.md#resolver-precision`](../methodology.md#resolver-precision). Same corpus pin and
  oracle procedure as Runs 0-5.
- **Scripted scorecard**: the eight tasks of [`2026-08.md`](2026-08.md) (L1, L2, R1, R2, I1, I2,
  E1, E2) with the same commands, the same corpus pin and the same grading of recall, precision,
  bytes and wall time, for the arms devscout (this release), `rg` (skilled) and `ast-grep`
  (R1/R2 only). The truth sets are the ones that document fixed; see "Scorecard grading" below.
- **Not measured**: the agentic (model-in-the-loop) round, which stays the 0.2.0 measurement of
  2026-08; the peer roster beyond `rg` and `ast-grep`; any corpus other than the pin; repeat
  variance.

## Registered predictions (2026-09-25, before any measurement)

### Resolver precision

Basis, three records, all on this corpus pin and oracle output:

- **(a)** Run 5 phase 3 of [`2026-09-resolver-precision.md`](2026-09-resolver-precision.md), the
  precise-tier refusals before the member-qualifier arity change.
- **(b)** The recorded effect of the member-qualifier arity change on the same procedure: precise
  edges 31234 to 31240 (all six added edges true positives, none removed or retargeted), guess
  edges 8996 to 9007 (tp -1, fp +12, guess `fp:wrong` 1798 to 1810, from six bare
  `SagaStateMachine.Behavior` qualifiers that now fall to the scored tier), structural `checked`
  42769 to 42786, graph edges 135901 to 135918; every precise false-positive class, `leaked`,
  partial file mismatch and all three recall figures unchanged.
- **(c)** [`2026-09-24-precise-wrong-target-attribution.md`](2026-09-24-precise-wrong-target-attribution.md),
  measured with a binary built from a commit whose `src/` differs from this release's only in
  comments and whose manifest differs only in the version string: graph sha256
  `f6523adde51480cf4fb9e051f529e7ab741c353063e3db1e59b018b6ee8da999`, precise 31240 / 31026 /
  214 / 0.993, `fp:no-site` 44, `fp:external` 0, `fp:wrong` 170, `structural` 0; oracle
  `refs.jsonl` sha256 `f9b579f0629a0b930f37723edab102d0a185fc07971582988cc4272e82f5c3a6`,
  `units.jsonl` sha256 `3b7137cfff70996059ae07cadd6ff55e315f9fdaa29ef01ce1c1f0a4bc2c8fc6`.

Because (c) and this release differ only in comments and the crate version, and the version is
not written into `graph.json`, the prediction is that this run reproduces (c) exactly and (a)
moved by exactly (b):

| # | Figure | Predicted | Basis |
| --- | --- | --- | --- |
| P1 | oracle `refs.jsonl` / `units.jsonl` sha256 | `f9b579f0…c3a6` / `3b7137cf…fc6`, 112190 records / 66924 sites, 56 units ok, 0 failed | (c); the oracle's walk is unchanged for these inputs |
| P2 | `graph.json` sha256 | `f6523add…a999`; 5634 files, 9951 defs, 135918 edges | (c), (b) |
| P3 | precise edges / tp / fp / precision | 31240 / 31026 / 214 / 0.993 | (c), (a)+(b) |
| P4 | precise `fp:no-site` / `fp:external` / `fp:wrong` / `structural` | 44 / 0 / 170 / 0 | (c), (a) |
| P5 | ext edges / tp / fp / precision | 2543 / 2521 / 22 / 0.991 (`fp:no-site` 5, `fp:external` 17, `fp:wrong` 0) | (a), flat under (b) |
| P6 | guess edges / tp / fp / precision | 9007 / 5085 / 3922 / 0.565 (`fp:no-site` 48, `fp:external` 2064, `fp:wrong` 1810) | (a)+(b) |
| P7 | recall precise / precise+ext / all (57607 sites) | 0.528 / 0.571 / 0.659 | (a), flat under (b) |
| P8 | recall by receiver: `ident` / `qualified` / `this` / `base` / `call` | 0.759 / 0.288 / 0.944 / 0.978 / 0.062 | (a) |
| P9 | external sites / silent-correct / leaked | 19153 / 18222 / 931 | (a), flat under (b) |
| P10 | structural impossible / checked | 2 / 42786 | (a)+(b) |
| P11 | fan-out 1 / 2 / 3 / 4+ | 26518 / 4959 / 1481 / 438 | (a) plus five new single-candidate precise sites and six sites with two new guess candidates; derived, not recorded, so the least certain row |
| P12 | partial file mismatch / ambiguous / edges outside universe | 155 / 137 / 733 | (a), (b) |
| P13 | top-20 FP targets | Run 5 phase 3's list and counts | the twelve new guess false positives split six and six over two targets, below the list's floor of 40 |
| P14 | top-20 missed targets | Run 5 phase 3's list and counts | (b) moves no recall figure |

Falsification: any precise-tier figure other than P3/P4 means the release tree resolves
differently from the attribution record's binary, and the difference is published and explained
before anything else. P2 missing while P3-P12 hold is read the same way, since the audit reads
only part of the graph.

### Scripted scorecard

The rg and ast-grep binaries, the corpus pin and the path prefix are those of 2026-08's 0.2.0
rerun, so their cells are predicted byte-identical to it. The devscout cells carry every release
since 0.2.0: `find` ordering and exact generic arity (0.3.0), heuristic tiers and recall (0.4.0),
`implements`/`overrides` edges and graph schema 3 (0.6.0), the precise-tier refusals, the
member-qualifier arity rule and the new `freshness` key on every `--json` answer (0.7.0). Their
grades are predicted, their bytes are not.

| id | devscout (predicted) | 0.2.0 rerun | rg (predicted) | ast-grep (predicted) |
| --- | --- | --- | --- | --- |
| L1 | correct, reach yes | correct, reach yes | correct, 3339 B | not attempted |
| L2 | correct, reach yes | correct, reach yes | correct, 623 B | not attempted |
| R1 | correct 1.000/0.947 under `--all` | correct 1.000/0.947 | correct 1.000/0.947, 9826 B | correct 1.000/0.947, 16574 B |
| R2 | correct 1.000/0.889 | correct 1.000/0.889 | correct 1.000/0.889, 1859 B | correct 1.000/0.889, 3065 B |
| I1 | partial, recall 1.0, rows over the 40-row cap, precision above rg's | partial 1.0/0.157 | partial 1.0/0.051, 156 files, 4 calls | not attempted |
| I2 | partial, recall 1.0, rows over the 25-row cap, precision above rg's | partial 1.0/0.098 | partial 1.0/0.060, 84 files, 4 calls | not attempted |
| E1 | partial, reach yes (the primary file still unnamed) | partial, reach yes | correct, 7368 B | not attempted |
| E2 | correct, reach yes | correct, reach yes | correct, 1431 B | not attempted |

Predicted totals: devscout 5 correct / 3 partial, rg 6 / 2, ast-grep 2 / 2 attempted, all as
2026-08's 0.2.0 rerun. Predicted band verdicts, unchanged: locate a tie on correctness with rg
faster; references a tie on correctness; impact to devscout on precision and tool calls;
e2e-retrieval to rg. Predicted: rg leads warm wall time in all four bands, and setup never
breaks even.

### Scorecard grading

Fixed here, before any 0.7.0 output exists, from 2026-08's definitions and its stored truth:

- **References.** Core set = the files naming the symbol as a whole word, minus its declaring
  file: R1 (`MessageUrn`) 18 files, R2 (`IJobService`) 8. `correct` at ≥75% of core with ≤2
  files outside core ∪ admissible (the declaring file is admissible); `partial` at ≥40%.
- **Impact.** Graded set = the seed's adjudicated hop-1 dependents: I1 (`IJobService.cs`) the
  same 8 files as R2's core, I2 (`IntervalRetryPolicy.cs`) the 5 files naming
  `IntervalRetryPolicy` beyond its declaring file. `correct` needs every graded file within the
  declared cap of 5x the graded set (40 and 25 rows); `partial` needs at least half.
- **Locate and e2e-retrieval.** `correct` names the primary file (`MessageUrn.cs`,
  `IntervalRetryContext.cs`, `InMemoryOutboxFilter.cs`, `BusHealthCheck.cs`). An output that
  omits the primary is `partial` when it names the other truth file 2026-08 credited, and a file
  set containing the 0.2.0 cell's carries that cell's grade.
- **Reach**, bytes (`tokens = ceil(bytes / 4)`), cold and warm-median-of-5 wall time, and tool
  calls as 2026-08 measured them. Before any 0.7.0 cell is graded, the grader is run over the
  stored 0.2.0 streams and must reproduce every 0.2.0 grade, recall and precision exactly.

`rg`'s impact cells are 2026-08's four-call chain (declared types of the seed, the files naming
them, the types declared in those files, the files naming any of those). 2026-08 reconstructed it
from stored streams; this run records the exact command lines with the results.

## 0.7.0 release run

Measured 2026-09-25 on the release commit, offline throughout. Every figure below comes from one
wiped-state run: fresh corpus copies, a fresh index, and an oracle regenerated from the release
tree rather than reused.

### Pin

```
Corpus            MassTransit/MassTransit @ 855cf1752c94ca9498e0c45ce8d09fdc9e957dd6
                  `git archive` export per arm, HEAD at the pin, tree clean, no remote, no .git/scout
Source            release commit ed6fc11f3b11e7dbc2a6b94c38b83a3860d777e7 (`devscout --version`: devscout 0.7.0)
Binary build      `git archive` export of that commit, `cargo build --release --locked -j 4 --offline`,
                  isolated HOME/TMPDIR/CARGO_TARGET_DIR, rustc 1.97.1 (8bab26f4f 2026-07-14),
                  aarch64-apple-darwin; the binary is pinned by commit, toolchain and command, since
                  its sha256 depends on the build directory
Oracle build      same export, `dotnet restore tools/scout-semantic --locked-mode`,
                  `dotnet build tools/scout-semantic --no-restore -c Release`, SDK 9.0.305
Oracle run        `dotnet restore MassTransit.sln -p:TargetFrameworks=net9.0` (8.6 s, from the local
                  package cache), then `dotnet run --no-restore --project tools/scout-semantic -c Release`
                  with `-p:TargetFrameworks=net9.0` (36 s); 56 units ok, 0 failed
refs.jsonl        sha256 f9b579f0629a0b930f37723edab102d0a185fc07971582988cc4272e82f5c3a6
                  112190 records / 66924 sites
units.jsonl       sha256 3b7137cfff70996059ae07cadd6ff55e315f9fdaa29ef01ce1c1f0a4bc2c8fc6
graph.json        sha256 f6523adde51480cf4fb9e051f529e7ab741c353063e3db1e59b018b6ee8da999
                  5634 files, 9951 defs, 135918 edges (43523 uses-member); graph.json and
                  fragments-v21.json byte-identical on the audit copy and the scorecard copy
Precise wrong-target `--fp-sites` rows, sorted, sha256
                  f9698caf6431061ff7bf5190cc9ccf7fd64f7cfde2241dfe7794042122dceb5b (170 rows)
```

Deviations from the planned environment: the restore ran with no network at all, so each of its
56 project restores logged `NU1900` (no vulnerability data), and those 56 lines are 56 of the
oracle's 62 workspace diagnostics; the other six are the `MassTransit.MartenIntegration.Tests`
target mismatch Run 0 recorded (three) and three project references without a matching metadata
reference. No unit failed, and the oracle output is byte-identical to the recorded digests, so
none of them moved a record. `dotnet run` took `--no-restore` so it could not reach for a feed.

### Audit text output (verbatim; `root` rewritten bench-relative, nothing else changed)

```
devscout audit --semantic  root bench/corpora/csharp  oracle 112190 records / 66924 sites  units ok 56 failed 0  method units
tier        edges     tp     fp   precision   fp:no-site  fp:external  fp:wrong  structural
precise     31240  31026    214       0.993           44            0       170           0
ext          2543   2521     22       0.991            5           17         0           0
guess        9007   5085   3922       0.565           48         2064      1810           0
recall (57607 in-graph member sites)  precise 0.528  precise+ext 0.571  all 0.659
  by receiver  ident 0.759  qualified 0.288  this 0.944  base 0.978  call 0.062
external sites 19153  silent-correct 18222  leaked 931
structural  impossible 2  checked 42786
fan-out  1: 26516  2: 4949  3: 1483  4+: 442
top fp targets   InMemoryDelayProvider 295  RoutingSlipExtensions 161  BusRegistrationContext 148  IBusRegistrationContext 148  IPerformanceCounter 148  NullPerformanceCounter 148  StatsDPerformanceCounter 148  Retry 124  InMemoryContainerTestFixture 83  ContainerTestHarness 78  TextTableOptions 65  IRegistrationConfigurator 53  IIndexedSagaProperty 50  IndexedSagaDictionary 50  IndexedSagaProperty 50  SuperComplexRequest 44  ConsumerPipeConfiguratorExtensions 42  RetryConfigurationExtensions 42  SagaPipeConfiguratorExtensions 42  ResponseHandlerConnectHandle 40
top missed       MassTransit.ConsumeContext 1021  MassTransit.BehaviorContext 674  MassTransit.Testing.IBaseTestHarness 621  MassTransit.Testing.ITestHarness 600  MassTransit.TransitionExtensions 449  MassTransit.SendContext 373  MassTransit.IStateMachineModifier 340  MassTransit.ThenExtensions 319  MassTransit.DependencyInjectionTestingExtensions 294  MassTransit.Testing.IReceivedMessageList 278  MassTransit.Testing.IPublishedMessageList 272  MassTransit.TestStateMachineExtensions 268  MassTransit.IRegistrationConfigurator 262  MassTransit.SagaConsumeContext 210  MassTransit.IPublishEndpoint 186  MassTransit.ISendEndpoint 178  MassTransit.Testing.AsyncElementListExtensions 170  MassTransit.IBusControl 161  MassTransit.ConsumerExtensions 158  MassTransit.Headers 145
unknown targets  class 3  record 1  struct 1
partial file mismatch 155
ambiguous 137
edges outside universe (not judged) 733
```

### `--json` tier objects (verbatim)

```json
{
  "precise": {
    "edges": 31240,
    "tp": 31026,
    "fp": 214,
    "precision": 0.993,
    "fp_no_site": 44,
    "fp_external_site": 0,
    "fp_wrong_target": 170,
    "structural": 0
  },
  "ext": {
    "edges": 2543,
    "tp": 2521,
    "fp": 22,
    "precision": 0.991,
    "fp_no_site": 5,
    "fp_external_site": 17,
    "fp_wrong_target": 0,
    "structural": 0
  },
  "guess": {
    "edges": 9007,
    "tp": 5085,
    "fp": 3922,
    "precision": 0.565,
    "fp_no_site": 48,
    "fp_external_site": 2064,
    "fp_wrong_target": 1810,
    "structural": 0
  }
}
```

### Recall by receiver kind

Denominator 57607 in-graph member sites, the same as Run 5.

| Receiver kind | Run 5 phase 3 | 0.7.0 release run |
| --- | --- | --- |
| `ident` | 0.759 | 0.759 |
| `qualified` | 0.288 | 0.288 |
| `this` | 0.944 | 0.944 |
| `base` | 0.978 | 0.978 |
| `call` | 0.062 | 0.062 |
| **all** | **0.659** (precise 0.528, precise+ext 0.571) | **0.659** (precise 0.528, precise+ext 0.571) |

`conditional` (`?.`): 717 records. `bare` (unqualified invocation): 7856 records. Both excluded
from the headline recall figure, as in every earlier run.

### External-receiver leak

19153 external sites: 18222 silent-correct, 931 leaked, unchanged from Run 5 phase 3. The six
`SagaStateMachine.Behavior` sites that gained guess edges are not external sites (the oracle binds
them to the in-tree static class `MassTransit.SagaStateMachine.Behavior`), so they add guess-tier
`fp:wrong`, not leak.

### Fan-out (candidate count per site)

| Run | 1 | 2 | 3 | 4+ |
| --- | --- | --- | --- | --- |
| Run 5 phase 3 | 26513 | 4953 | 1481 | 438 |
| 0.7.0 release run | 26516 | 4949 | 1483 | 442 |

### Top-20 FP targets, by short name

Identical to Run 5 phase 3, name for name and count for count. The twelve new guess false
positives (six each on `ActivityBehaviorBuilder` and `CatchBehaviorBuilder`) stay below the
list's floor of 40.

| Target | FP count |
| --- | --- |
| InMemoryDelayProvider | 295 |
| RoutingSlipExtensions | 161 |
| BusRegistrationContext | 148 |
| IBusRegistrationContext | 148 |
| IPerformanceCounter | 148 |
| NullPerformanceCounter | 148 |
| StatsDPerformanceCounter | 148 |
| Retry | 124 |
| InMemoryContainerTestFixture | 83 |
| ContainerTestHarness | 78 |
| TextTableOptions | 65 |
| IRegistrationConfigurator | 53 |
| IIndexedSagaProperty | 50 |
| IndexedSagaDictionary | 50 |
| IndexedSagaProperty | 50 |
| SuperComplexRequest | 44 |
| ConsumerPipeConfiguratorExtensions | 42 |
| RetryConfigurationExtensions | 42 |
| SagaPipeConfiguratorExtensions | 42 |
| ResponseHandlerConnectHandle | 40 |

### Top-20 missed targets, by id

Identical to Run 5 phase 3.

| Target id | Missed count |
| --- | --- |
| MassTransit.ConsumeContext | 1021 |
| MassTransit.BehaviorContext | 674 |
| MassTransit.Testing.IBaseTestHarness | 621 |
| MassTransit.Testing.ITestHarness | 600 |
| MassTransit.TransitionExtensions | 449 |
| MassTransit.SendContext | 373 |
| MassTransit.IStateMachineModifier | 340 |
| MassTransit.ThenExtensions | 319 |
| MassTransit.DependencyInjectionTestingExtensions | 294 |
| MassTransit.Testing.IReceivedMessageList | 278 |
| MassTransit.Testing.IPublishedMessageList | 272 |
| MassTransit.TestStateMachineExtensions | 268 |
| MassTransit.IRegistrationConfigurator | 262 |
| MassTransit.SagaConsumeContext | 210 |
| MassTransit.IPublishEndpoint | 186 |
| MassTransit.ISendEndpoint | 178 |
| MassTransit.Testing.AsyncElementListExtensions | 170 |
| MassTransit.IBusControl | 161 |
| MassTransit.ConsumerExtensions | 158 |
| MassTransit.Headers | 145 |

### Predictions vs. actual

| # | Predicted | Actual | Verdict |
| --- | --- | --- | --- |
| P1 | oracle digests `f9b579f0…c3a6` / `3b7137cf…fc6`, 112190 / 66924, 56 ok, 0 failed | identical | **HOLD** |
| P2 | `graph.json` `f6523add…a999`; 5634 / 9951 / 135918 | identical, on both corpus copies | **HOLD** |
| P3 | precise 31240 / 31026 / 214 / 0.993 | 31240 / 31026 / 214 / 0.993 | **HOLD** |
| P4 | precise 44 / 0 / 170 / 0 | 44 / 0 / 170 / 0 | **HOLD** |
| P5 | ext 2543 / 2521 / 22 / 0.991 (5 / 17 / 0) | identical | **HOLD** |
| P6 | guess 9007 / 5085 / 3922 / 0.565 (48 / 2064 / 1810) | identical | **HOLD** |
| P7 | recall 0.528 / 0.571 / 0.659 | 0.528 / 0.571 / 0.659 | **HOLD** |
| P8 | 0.759 / 0.288 / 0.944 / 0.978 / 0.062 | identical | **HOLD** |
| P9 | 19153 / 18222 / 931 | 19153 / 18222 / 931 | **HOLD** |
| P10 | structural 2 / 42786 | 2 / 42786 | **HOLD** |
| P11 | fan-out 26518 / 4959 / 1481 / 438 | 26516 / 4949 / 1483 / 442 | **MISS** |
| P12 | 155 / 137 / 733 | 155 / 137 / 733 | **HOLD** |
| P13 | top-20 FP list as Run 5 phase 3 | identical | **HOLD** |
| P14 | top-20 missed list as Run 5 phase 3 | identical | **HOLD** |

Thirteen of fourteen hold, and the release tree resolves exactly as the attribution record's
binary did: the same graph bytes, the same oracle bytes, the same 170 precise wrong-target rows.

**P11, explained.** The audit counts fan-out per `(file, line)` over every judged edge on that
line, not per new edge. The prediction treated each of the eleven lines that gained edges as a
line that had none before. Five did (the new single-candidate precise edges), but the six
`SagaStateMachine.Behavior` lines already carried one or two edges each, so their two new guess
edges moved them up the histogram instead of adding new two-candidate lines: two lines from 1 to
3 and four from 2 to 4. Applied to Run 5 phase 3, those moves (1: +5 -2, 2: -4, 3: +2, 4+: +4)
give 26516 / 4949 / 1483 / 442, the measured row exactly, with the per-line counts read from this
run's `graph.json`.

### Before and after

| Metric | Run 5 phase 3 | 0.7.0 release run |
| --- | --- | --- |
| precise edges | 31234 | 31240 |
| precise precision | 0.993 | 0.993 |
| precise `fp:no-site` / `fp:external` / `fp:wrong` / `structural` | 44 / 0 / 170 / 0 | 44 / 0 / 170 / 0 |
| ext edges / precision | 2543 / 0.991 | 2543 / 0.991 |
| guess edges / precision | 8996 / 0.565 | 9007 / 0.565 |
| leaked external sites | 931 | 931 |
| recall precise / precise+ext / all | 0.528 / 0.571 / 0.659 | 0.528 / 0.571 / 0.659 |

Worse: guess-tier false positives rise by twelve (3910 to 3922, `fp:wrong` 1798 to 1810), all on
six bare `SagaStateMachine.Behavior` qualifiers. `SagaStateMachine` there is a segment of the
enclosing namespace; the member-qualifier arity rule reads the bare qualifier as a
zero-type-argument type, finds none in the tree (the only same-named type is the generic
interface), and the reference falls to the scored tier, which guesses two in-tree types with a
`Behavior` property. Precise edges rise by six, all true positives. Nothing else moved.

## Scripted scorecard, devscout 0.7.0

Same eight tasks, arms, commands and grading as [`2026-08.md`](2026-08.md); only the binary and
the corpus copy changed.

```
Date            2026-09-25
Corpus          MassTransit/MassTransit @ 855cf1752c94ca9498e0c45ce8d09fdc9e957dd6 — `git archive`
                export at bench/corpora/csharp, HEAD at the pin; 5634 files indexed
Tool version    devscout 0.7.0, release commit ed6fc11f3b11e7dbc2a6b94c38b83a3860d777e7
Build           cargo build --release --locked -j 4 --offline, rustc 1.97.1 (8bab26f4f 2026-07-14),
                aarch64-apple-darwin
Host            macOS (Darwin 25.2.0), Apple M2 Max, arm64; 12 cores, 64 GiB RAM — NOT idle, see
                Deviations
Baseline        ripgrep 15.2.0 (+PCRE2 10.45);  peer run: ast-grep 0.45.1
Bench root      bench/ (throwaway); verbatim streams stored as raw bytes
Isolation       HOME, TMPDIR, SCOUT_REGISTRY and SCOUT_CONTENT_DB redirected under the bench root
Network         none, setup included
Reps            cold 1 + warm median of 5 per cell, in-process timer around each subprocess
Deviations      1. The host was shared: its load averages read 15.6 / 12.5 / 15.9 (1, 5, 15 min)
                   right after the timing pass. Both arms' wall times are higher than in 2026-08
                   (rg's mean warm 131.7 ms against 93.6 ms there), so compare wall time only between
                   arms of this run.
                2. Streams are stored byte for byte. The 0.2.0 rerun's stored rg streams for L1 and R1
                   are one byte shorter than the same output here (3339 / 9826 against 3340 / 9827):
                   one matched line ends in a carriage return from a CRLF source file, which that
                   capture dropped. The first 2026-08 round measured 3340 / 9827 as well.
                3. rg's impact chain is recorded verbatim below. It reproduces the 0.2.0 rerun's four
                   intermediate and final file sets exactly.
                4. The truth sets were rebuilt by the rule registered above and checked by regrading
                   the stored 0.1.0 and 0.2.0 streams: every grade, recall, precision, file count and
                   reach verdict of both rounds came out identical.
```

| Arm | Index build | On-disk bytes | Break-even |
| --- | --- | --- | --- |
| devscout | 3.020 s, median of 5 full cold rebuilds (min 2.970, max 3.257), under the load above | 108,505,951 = graph 47,140,003 + fragments 59,329,911 + fragments-index 538,994 + project units 16,656 + manifest 1,480,217 + state 170 | never here |
| rg / ast-grep | none | 0 | n/a |

Never, for the same reason as 2026-08: devscout trails on both per-task axes, mean warm 215.7 ms
against rg's 131.7 ms and mean payload 2058 against 1572 tokens.

### Every cell

`D` = `devscout -C bench/corpora/csharp`, `C` = `bench/corpora/csharp`; commands as 2026-08. Grades
read `grade recall/precision, bytes`.

| id | kind | devscout 0.7.0 | devscout 0.2.0 (2026-08 rerun) | rg | ast-grep |
| --- | --- | --- | --- | --- | --- |
| L1 | locate | correct, 2692 B, reach yes | correct, 2692 B, reach yes | correct, 3340 B, reach yes | not attempted |
| L2 | locate | correct, 1393 B, reach yes | correct, 1393 B, reach yes | correct, 623 B, reach yes | not attempted |
| R1 | references | correct 1.000/0.947, 19 files, 10172 B under `--all`; plain form partial 0.722/0.929, 14 files, 5618 B | correct 1.000/0.947, 19 files, 8655 B; plain 4773 B | correct 1.000/0.947, 9827 B | correct 1.000/0.947, 16574 B |
| R2 | references | **partial 1.000/0.667, 12 files, 5189 B** | correct 1.000/0.889, 9 files, 3741 B | correct 1.000/0.889, 1859 B | correct 1.000/0.889, 3065 B |
| I1 | impact | partial 1.000/0.157, 51 rows over a 40-row cap, 15719 B | partial 1.000/0.157, 51 rows, 14038 B | partial 1.000/0.051, 156 files, 16294 B, 4 calls | not attempted |
| I2 | impact | partial 1.000/0.098, 51 rows over a 25-row cap, 15959 B | partial 1.000/0.098, 51 rows, 14753 B | partial 1.000/0.060, 84 files, 9561 B, 4 calls | not attempted |
| E1 | e2e-retrieval | partial, 36 files, 8341 B, reach yes | partial, 25 files, 8899 B, reach yes | correct, 7368 B | not attempted |
| E2 | e2e-retrieval | correct, 6392 B, reach yes | correct, 6392 B, reach yes | correct, 1431 B | not attempted |

**devscout 4 correct / 4 partial, was 5 / 3. rg 6 / 2 and ast-grep 2 / 2 attempted, both
unchanged.** Every rg and ast-grep cell returns the file set and bytes of 2026-08 (deviation 2
aside). What moved on the devscout side:

- **R2 drops to partial.** The answer adds three files that never name `IJobService`:
  `JobServiceRegistration.cs:63` and `JobServiceConfigurator.cs:39`
  (`Settings.JobService.ConfigureSuperviseJobConsumer(...)`) and
  `JobServiceConsumerConfigurationObserver.cs:61` (`_settings.JobService.RegisterJobType(...)`).
  Opened at the call site, each is a real call of an `IJobService` member through
  `JobServiceSettings.JobService`, a property declared `IJobService`, emitted as a precise
  `uses-member` edge. They are exactly the reference class
  [`methodology.md`](../methodology.md#task-taxonomy) says a text-built truth set cannot hold, so
  the registered rule counts them as three files outside core ∪ admissible, one more than
  `correct` allows. The grade stands as registered; the three files are correct references.
- **I1 and I2** keep their grades, recall and precision, and the same 51-row size, but swap part
  of the hop-2 tail (15 of 51 files in I1, 27 in I2) and grow by 12% and 8% in bytes. Every
  graded hop-1 file is still present.
- **E1** still never names `InMemoryOutboxFilter.cs`. Its file set changed (19 files added, 8 of
  the 0.2.0 set gone, among them three Markdown pages), so the carry rule does not apply and the
  cell was adjudicated. 2026-08 recorded its second truth file only as a recall of 0.5, not by
  name; of the fourteen files that could have been it (named by both the 0.2.0 devscout cell and
  rg), ten are still named, including `InMemoryOutboxConsumeContext.cs`, which holds the
  clear-to-send gate that defers publishes until the consumer completes. Graded partial.
- **L1, L2 and E2** return the 0.2.0 file sets at the same byte counts, in a different row order.
- The four `--json` answers carry the 0.7.0 `schema_version`, `outcome` and `freshness` keys
  (`"state": "fresh"` on every one), part of each byte increase.

### Correctness, cost, wall time, reach

| Arm | correct | partial | wrong | not attempted | correct+partial |
| --- | --- | --- | --- | --- | --- |
| devscout | 4 | 4 | 0 | 0 | 8 / 8 |
| rg | 6 | 2 | 0 | 0 | 8 / 8 |
| ast-grep | 2 | 0 | 0 | 6 — locate, impact, e2e-retrieval; "no symbol table or cross-file graph" | 2 / 2 attempted |

Median of per-task ratios (rg bytes / devscout bytes) **0.74**, as in both 2026-08 rounds. Ratio of
pooled medians (5354 / 7366.5) **0.73**, was 0.71.

| Band | devscout cold / warm ms | rg cold / warm ms | reach: devscout | reach: rg |
| --- | --- | --- | --- | --- |
| locate | 178 / 173 | 186 / 146 | yes | yes |
| references | 254 / 263 | 137 / 90 | yes | yes |
| impact | 266 / 264 | 242 / 199 (4 calls) | no | no |
| e2e-retrieval | 165 / 163 | 113 / 92 | yes | no |

Band figures are the mean of the band's two tasks. rg's L1 warm median (193.8 ms, 70 ms in
2026-08) is the one cell far off its own history, and the load in deviation 1 is the only
explanation on record.

### Where peers win

Derived from the grid by the harness, one line per band, the arm that leads each metric:

- **locate** — correctness tie (devscout, rg); payload bytes rg; warm wall time rg; tool calls tie;
  reach tie.
- **references** — correctness rg and ast-grep; precision rg and ast-grep; payload bytes rg; warm
  wall time rg; tool calls tie; reach tie.
- **impact** — correctness tie (both partial); precision devscout (0.157 / 0.098 against 0.051 /
  0.060); tool calls devscout (1 against 4); payload bytes rg; warm wall time rg; reach tie (neither).
- **e2e-retrieval** — correctness rg; payload bytes rg; warm wall time rg; reach devscout; tool calls
  tie.

rg leads warm wall time in all four bands, payload bytes in all four, and correctness in
`references` and `e2e-retrieval`. devscout leads only `impact` precision and tool calls, and
`e2e-retrieval` reach. ast-grep matches rg's reference file sets and grades at 4.2x rg's warm time
on `MessageUrn` (378.6 ms against 90.9 ms).

### Scorecard predictions vs. actual

| Registered | Actual | Verdict |
| --- | --- | --- |
| L1, L2, E2 devscout correct, reach yes | as predicted | **HOLD** |
| R1 devscout correct 1.000/0.947 under `--all` | as predicted | **HOLD** |
| R2 devscout correct 1.000/0.889 | partial 1.000/0.667 | **MISS** — the three inference-only references above |
| I1, I2 devscout partial, recall 1.0, over the cap, precision above rg's | as predicted | **HOLD** |
| E1 devscout partial, reach yes | as predicted, by adjudication | **HOLD** |
| rg and ast-grep cells byte-identical to the 0.2.0 rerun | identical file sets and bytes, except L1 and R1 rg at +1 B | **MISS by one byte on two cells** — the dropped carriage return in deviation 2 |
| totals devscout 5 / 3, rg 6 / 2, ast-grep 2 / 2 | devscout 4 / 4; rg and ast-grep as predicted | **MISS** on devscout (R2) |
| band verdicts: locate tie with rg faster; references tie on correctness; impact to devscout; e2e-retrieval to rg | locate, impact and e2e-retrieval as predicted; references to rg and ast-grep | **MISS** on references (R2) |
| rg leads warm wall time in all four bands; setup never breaks even | as predicted | **HOLD** |

### rg impact chain, verbatim

`DECL` stands for the declaration pattern, passed with `-e` as written:
`^\s*(?:(?:public|internal|private|protected|static|sealed|abstract|partial|readonly|unsafe|new|file)\s+)*(?:class|interface|struct|record|enum)\s+(\w+)`.
Call 2 takes the type names call 1 prints, call 3 the files call 2 lists minus the seed, and call
4 the type names call 3 prints; the graded answer is call 4's file list.

```
# I1
rg -n -e "$DECL" bench/corpora/csharp/src/MassTransit/JobService/JobService/IJobService.cs
rg -l -w -g '*.cs' -e IJobService bench/corpora/csharp
rg -n -e "$DECL" <the 8 files call 2 lists beyond the seed>
rg -l -w -g '*.cs' -e IJobTypeRegistration -e InstanceJobServiceSettings -e JobConsumerMessageConnector \
  -e JobService -e JobServiceBusObserver -e JobServiceOptions -e JobServiceSettings \
  -e JobTypeRegistration -e StartJobConsumer -e SuperviseJobConsumer bench/corpora/csharp
# I2
rg -n -e "$DECL" bench/corpora/csharp/src/MassTransit/RetryPolicies/IntervalRetryPolicy.cs
rg -l -w -g '*.cs' -e IntervalRetryPolicy bench/corpora/csharp
rg -n -e "$DECL" <the 5 files call 2 lists beyond the seed>
rg -l -w -g '*.cs' -e IntervalRetryContext -e IntervalRetryPolicyContext -e Retry \
  -e RetryConfigurationExtensions -e RetryConfigurator -e RetryObserver -e TestContext \
  -e TestRetryContext -e TestRetryPolicy -e TestRetryPolicyContext -e Using_the_retry_filter bench/corpora/csharp
```

The four outputs are stored in order, separated by `----` lines, and the payload is all four.
