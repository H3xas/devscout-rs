# Results — devscout 0.8.0 release run, 2026-09-25

> **Single run, no repeats.** One measurement of the final 0.8.0 source tree, in four parts: the
> default-lane resolver-precision audit by the method of
> [`2026-09-25-release-0.7.0.md`](2026-09-25-release-0.7.0.md) (itself the method of Runs 0-5 in
> [`2026-09-resolver-precision.md`](2026-09-resolver-precision.md)); the reach of the new `bus-hop`
> edges; the opt-in enriched lane by the method of Runs 7 and 8 in that same document; and the
> scripted eight-task scorecard of [`2026-08.md`](2026-08.md). Run 8 was measured before the
> enrichment code was rebased onto the 0.7.0 line and carries no figure at this release's tree;
> this document measures that tree. Every prediction below was committed before any figure in this
> document was measured. No earlier document is edited.

## Environment

Filled in with the results. Planned values:

```
Date            2026-09-25
Corpus          MassTransit/MassTransit @ 855cf1752c94ca9498e0c45ce8d09fdc9e957dd6 (bench/corpus.lock, registered)
Tool version    devscout 0.8.0, built from a `git archive` export of the release commit
Comparison      devscout 0.7.0, rebuilt the same way from ed6fc11f3b11e7dbc2a6b94c38b83a3860d777e7
                (the commit the 0.7.0 release run measured), for the identity checks below
Build           cargo build --release --locked -j 4 --offline, rustc 1.97.1 (8bab26f4f 2026-07-14), aarch64-apple-darwin
Oracle          tools/scout-semantic from the 0.8.0 export, SDK 9.0.305 (the tree's default host)
Host            macOS (Darwin 25.2.0), Apple M2 Max, arm64; 12 cores, 64 GiB RAM
Baseline        ripgrep 15.2.0 (+PCRE2 10.45); peer: ast-grep 0.45.1 (the versions 2026-08 and the 0.7.0 run used)
Bench root      bench/ (throwaway; nothing installed globally)
Isolation       HOME, TMPDIR, CARGO_TARGET_DIR, SCOUT_REGISTRY and SCOUT_CONTENT_DB redirected
                under the bench root
Network         none: every step offline, the corpus restore included
Reps            audit 1 per lane; scorecard cold 1 + warm median of 5 per cell, as 2026-08
```

## Corpus pin and setup

| Language | Repository | Pin | License | Status |
| --- | --- | --- | --- | --- |
| C# | MassTransit/MassTransit | `855cf1752c94ca9498e0c45ce8d09fdc9e957dd6` | Apache-2.0 | registered — measured |

The corpus is prepared exactly as in the 0.7.0 run: a `git archive` export of the pinned commit,
given a git directory whose `HEAD` is that commit, so each copy reads as a clean checkout of the
pin. Each arm gets a fresh copy, none carries a `.git/scout` directory, and no existing checkout is
mapped.

```sh
git archive <release commit> | tar -x -C "$EXPORT"
CARGO_TARGET_DIR="$TARGET" cargo build --release --locked -j 4 --offline      # in $EXPORT
dotnet build tools/scout-semantic -c Release                                  # in $EXPORT
dotnet restore "$ORACLE_CORPUS/MassTransit.sln" -p:TargetFrameworks=net9.0
dotnet run --project tools/scout-semantic -c Release -- "$ORACLE_CORPUS/MassTransit.sln" \
  --root "$ORACLE_CORPUS" --out "$ORACLE/refs.jsonl" --units "$ORACLE/units.jsonl" \
  -p:TargetFrameworks=net9.0
cd "$CORPUS" && "$TARGET/release/devscout" map .
"$TARGET/release/devscout" audit --semantic "$ORACLE/refs.jsonl" --units "$ORACLE/units.jsonl"
"$TARGET/release/devscout" audit --semantic "$ORACLE/refs.jsonl" --units "$ORACLE/units.jsonl" --json
```

The enriched lane then continues on the same corpus copy with the remaining commands of
[`bench/semantic-enriched.sh`](../../../bench/semantic-enriched.sh): the oracle in
`--emit compiler-facts` mode (`--tfm net9.0 -p Configuration=Debug -p Platform=AnyCPU`, plus the
forwarded `-p:TargetFrameworks=net9.0`), `devscout compiler-facts import`, `devscout map .`, and
both audits again against the same `refs.jsonl`/`units.jsonl`. The scorecard runs from the bench
root with its own copy at `bench/corpora/csharp`, as in 2026-08 and the 0.7.0 run, and never sees
a compiler-facts artifact.

## What this measures / does not measure

- **Default lane, resolver precision**: the same figures, tables and pins as the 0.7.0 run, with
  the metric definitions, join rule and exclusions of
  [`methodology.md#resolver-precision`](../methodology.md#resolver-precision). `audit` scores
  `uses-member` edges only, so the new `bus-hop` edges are **excluded** from every precision and
  recall figure: they are candidate type relationships between a publish site and a handler
  declaration, and the oracle has no record kind that could judge one.
- **Bus-hop reach**: how many `bus-hop` edges the pinned corpus gains, their handler-evidence
  words, how many handlers each message reaches, how concentrated the hops are, whether the
  corpus supplied its own handler vocabulary, and that `--no-bus` answers exactly as 0.7.0 did.
  This is reach, not precision: as the changelog states, behaviour precision and recall of these
  routes are unmeasured, and this document measures neither.
- **Enriched lane (opt-in)**: the Run 7/8 tables (the enriched-lane audit, the tier-by-tier change,
  the registered shipping gate) at this tree, against a freshly produced and freshly admitted
  compiler-facts artifact. Like Runs 7 and 8 this is **consumer conformance only**: both lanes are
  scored against the same oracle output, not against independently verified truth.
- **Scripted scorecard**: the eight tasks of [`2026-08.md`](2026-08.md) (L1, L2, R1, R2, I1, I2,
  E1, E2) with the same commands, corpus pin and grading as the 0.7.0 run, for the arms devscout
  (this release, default lane), `rg` (skilled) and `ast-grep` (R1/R2 only).
- **Not measured**: the agentic (model-in-the-loop) round, which stays the preliminary 0.2.0
  measurement of 2026-08; the peer roster beyond `rg` and `ast-grep`; any corpus other than the
  pin; repeat variance.

## Registered predictions (2026-09-25, before any measurement)

### Default lane

Basis, three facts about this tree, each checked in the source before predicting:

- **(a)** `audit` reads only `uses-member` edges from `graph.json`; `bus-hop` edges are appended
  after every other kind, and the bus-hop feature changes no other edge kind or count (the
  changelog's "Every non-bus edge kind and count is unchanged from the same commit's non-bus map").
- **(b)** The compiler-fact enrichment is opt-in: with no admitted artifact, `map` produces the
  syntax-only graph.
- **(c)** The oracle's walker is unchanged since 0.7.0. Two things around it changed: a restore
  step now runs before the walk, and it acts only on a project whose assets file is missing; and
  the tool no longer ships its own copies of three MSBuild assemblies, so the registered SDK
  supplies them. This corpus is restored with `-p:TargetFrameworks=net9.0` before the walk, so
  every project has its assets file and the restore step should touch nothing; the walk runs
  under SDK 9.0.305, which the old copies never failed under (the 0.7.0 run used it), and the
  failure the change fixes needs a 10.0 SDK. The prediction is therefore that the walk, and so
  the oracle output, is unchanged.

`audit`'s text output now names the lane in its header (`lane syntax`) and carries an `unjudged`
column, and its `--json` tier objects an `unjudged` key and a top-level `lane` key. Those are
format changes, not figures:

| # | Figure | Predicted | Basis |
| --- | --- | --- | --- |
| P1 | oracle `refs.jsonl` / `units.jsonl` sha256 | `f9b579f0…c3a6` / `3b7137cf…fc6`, 112190 records / 66924 sites, 56 units ok, 0 failed; the restore step restores no project | (c) |
| P2 | `graph.json` | not byte-identical to 0.7.0's `f6523add…a999`: it gains the `bus-hop` edges, appended last, and the `stats` keys `bus-hop` and `bus_vocabulary_derived`. With those edges and keys removed it equals the graph the rebuilt 0.7.0 binary writes for the same corpus copy (5634 files, 9951 defs, 135918 edges, the same order), and that graph is itself `f6523add…a999` | (a), (b) |
| P3 | precise / ext / guess tier objects | 31240 / 31026 / 214 / 0.993 (44 / 0 / 170 / 0); 2543 / 2521 / 22 / 0.991 (5 / 17 / 0 / 0); 9007 / 5085 / 3922 / 0.565 (48 / 2064 / 1810 / 0); `unjudged` 0 on every tier; `lane` `syntax` | (a), (b); the 0.7.0 run |
| P4 | recall precise / precise+ext / all (57607 sites); by receiver | 0.528 / 0.571 / 0.659; 0.759 / 0.288 / 0.944 / 0.978 / 0.062 | the 0.7.0 run |
| P5 | external sites / silent-correct / leaked; structural impossible / checked | 19153 / 18222 / 931; 2 / 42786 | the 0.7.0 run |
| P6 | fan-out; partial file mismatch / ambiguous / outside universe; unknown targets | 26516 / 4949 / 1483 / 442; 155 / 137 / 733; class 3, record 1, struct 1 | the 0.7.0 run |
| P7 | top-20 FP and top-20 missed lists; sorted `--fp-sites` precise wrong-target rows | the 0.7.0 run's lists and counts; 170 rows, sha256 `f9698caf…5b` | the 0.7.0 run |

Falsification: any figure in P3-P7 other than predicted means a non-bus change reached the
default graph, and it is published and explained before anything else.

### Bus-hop reach

Basis: the changelog's own figures for this corpus pin ("23,687 candidate hops over 171 distinct
messages, of which one shared test message accounts for 23,068 across 79 handlers"; an array of
that message "reaching only the two handlers declared against the array: 4 hops from its 2
publish sites"), recorded before the release commit and not re-measured since. Nothing that
landed after them touches the bus code or the type ladder it resolves messages through.

| # | Figure | Predicted |
| --- | --- | --- |
| B1 | `bus-hop` edges in `graph.json` and `stats["bus-hop"]` | 23687, both |
| B2 | distinct messages (the edge's `message`, array suffix included) | 171 |
| B3 | hops on the shared test message `MassTransit.TestFramework.Messages.PingMessage` | 23068 (share 0.974), 79 distinct handlers |
| B4 | the array of that message | 4 hops from 2 publish sites, 2 handlers |
| B5 | distinct handlers per message: max / median | 79 / 1. The median is the least certain row: it rests on most other messages being test-local, with one consumer declared beside the publish |
| B6 | hops by evidence word | `base-arg` at least 95% of all hops (the shared message's 79 handlers name it on their base lists); each of `nested-base-arg`, `mediator-request` and `property-arg` under 5%; whether each appears at all is not predicted |
| B7 | `stats.bus_vocabulary_derived` | `true`: the corpus registers its handlers through one-type-argument registrations |
| B8 | `--no-bus` answers of the registered seeds below, 0.8.0 against the rebuilt 0.7.0 binary, same corpus copy | byte-identical, text and `--json` |

Registered seeds for B8, each asked of both binaries after the same `map`:

```
refs    MessageUrn | IJobService | IntervalRetryPolicy | PingMessage | PongMessage        (text and --json)
impact  src/MassTransit/JobService/JobService/IJobService.cs                              (--hops 2, text and --json)
        src/MassTransit/RetryPolicies/IntervalRetryPolicy.cs
        src/MassTransit.TestFramework/Messages/PingMessage.cs
        src/MassTransit.TestFramework/Messages/PongMessage.cs
```

The same seeds are also asked of 0.8.0 without `--no-bus`, and every difference from 0.7.0 is
reported as reach; those answers are not predicted.

### Enriched lane (opt-in)

Basis: Run 8's enriched table in
[`2026-09-resolver-precision.md`](2026-09-resolver-precision.md), and three changes between the
revision it measured and this tree:

- **(d)** The enrichment code now sits on the 0.7.0 resolver. References no compiler fact
  confirms still go through the syntax ladder, so the residual `precise`/`ext`/`guess` rows
  inherit 0.7.0's precise-tier refusals and the member-qualifier arity rule. In the syntax lane
  those moved precise `fp:external` 28 to 0, `fp:wrong` 288 to 170, `structural` 2 to 0, and guess
  `fp:external` 2101 to 2064 (Run 6 against the 0.7.0 run). Run 8's residual precise row has
  exactly 28 `fp:external`, the syntax lane's pre-refusal figure, so those are predicted to go.
- **(e)** At a compiler-discovered site, two confirmed same-type overloads now project as two
  distinct edges instead of nothing. On this corpus Run 8 located exactly eight such sites, all at
  `MessageFactory.Create`, each an oracle true positive before the change that made them project
  nothing.
- **(f)** The same-context override, the compiler-facts occurrence walk and the audit's scoring are
  otherwise unchanged; the `semantic` row and every recall figure do not depend on the syntax
  ladder's residual answers at the sites a fact covers.

| # | Figure | Run 8 | Predicted | Basis |
| --- | --- | --- | --- | --- |
| N1 | compiler-facts run | 34161 symbols, 21 diagnostics, 1 incomplete unit; 163606 occurrences admitted (161776 confirmed, 1812 unresolved, 18 ambiguous) | identical | (f); oracle input as P1 |
| N2 | admission; `lane` | admitted, coverage incomplete (1 unit); `enriched` | identical | (f) |
| N3 | `semantic` row | 46445 / 46357 / 88 / 0.998 (0 / 85 / 3 / 0) | identical | (f) |
| N4 | `semantic-discovered` row | 46082 / 8075 / 2406 / 0.770, `unjudged` 35601 | 46098 / 8091 / 2406 / 0.771, `unjudged` 35601 | (e): eight sites, two true-positive edges each |
| N5 | residual `precise` row | 3982 / 3859 / 123 / 0.969 (44 / 28 / 51 / 0) | `fp:external` 0, `fp:no-site` 44, `fp:wrong` at most 51, `structural` 0; so fp at most 95 and precision at least 0.975; edge and tp counts fall, magnitudes not predicted | (d) |
| N6 | residual `ext` and `guess` rows | 387 / 0.943; 3567 / 0.292 (`fp:external` 2097) | move with the syntax lane's Run 6 to 0.7.0 change: guess `fp:external` falls; magnitudes not predicted | (d) |
| N7 | recall precise / precise+ext / all | 0.848 / 0.854 / 0.891 | 0.848 / 0.854 / 0.891, each within 0.001 | (f) |
| N8 | `bus-hop` edges in the enriched graph | — | 23687, identical to the default lane's | (a); the bus code never reads compiler facts |
| N9 | `map --no-semantic` on the enriched copy | — | a `graph.json` byte-identical to the default lane's | (b) |

Registered shipping gate, re-evaluated at this tree against this tree's own syntax lane (P3, P4:
precise 0.993, precise+ext 0.571), as Run 8 did against its own:

| # | Criterion | Predicted |
| --- | --- | --- |
| G-1 | recall precise+ext gain at least +0.10 absolute and at least 0.70 | met: 0.854, +0.283 |
| G-2 | union of the enriched `precise` and `semantic` rows not more than 0.005 below the syntax lane's precise figure | met, the union above the syntax figure (about 0.996 against 0.993) |
| G-3 | cold acquisition at most 300 s wall | met |
| G-4 | cold acquisition at most 2x `dotnet build -c Release` of the same solution | not demonstrated: the whole-solution Release build under SDK 9.0.305 with `-p:TargetFrameworks=net9.0` is predicted to fail the same two projects as in Run 8, so the ratio can only be taken against the subset that builds |

G-4 is re-measured as Run 8 did: the build server shut down first, then three runs each of the
acquisition command and of `dotnet build MassTransit.sln -c Release --no-restore
-p:TargetFrameworks=net9.0`, on a corpus copy of its own.

### Scripted scorecard

The rg and ast-grep binaries, the corpus pin and the path prefix are the 0.7.0 run's, so every
rg and ast-grep cell is predicted byte-identical to that run's stored streams. On the devscout
side, `find` ranks by an inbound count that skips `bus-hop` edges, and `refs` prints a bus-hop
block only for a symbol that sends or handles a message; `impact` is the only scorecard verb whose
answer the default bus traversal can change here, because a file it reaches that declares a
handler brings that message's publishers into the walk.

| id | devscout (predicted) | 0.7.0 run | rg (predicted) | ast-grep (predicted) |
| --- | --- | --- | --- | --- |
| L1 | correct, reach yes; byte-identical to 0.7.0 | correct, 2692 B | correct, 3340 B | not attempted |
| L2 | correct, reach yes; byte-identical | correct, 1393 B | correct, 623 B | not attempted |
| R1 | correct 1.000/0.947 under `--all`; byte-identical (plain form likewise) | correct, 10172 B; plain 5618 B | correct 1.000/0.947, 9827 B | correct 1.000/0.947, 16574 B |
| R2 | partial 1.000/0.667, the three inference-only files; byte-identical | partial, 5189 B | correct 1.000/0.889, 1859 B | correct 1.000/0.889, 3065 B |
| I1 | partial, recall 1.0, rows over the 40-row cap, precision above rg's; bytes not predicted | partial 1.000/0.157, 15719 B | partial 1.000/0.051, 16294 B, 4 calls | not attempted |
| I2 | partial, recall 1.0, rows over the 25-row cap, precision above rg's; bytes not predicted | partial 1.000/0.098, 15959 B | partial 1.000/0.060, 9561 B, 4 calls | not attempted |
| E1 | partial, reach yes; byte-identical | partial, 8341 B | correct, 7368 B | not attempted |
| E2 | correct, reach yes; byte-identical | correct, 6392 B | correct, 1431 B | not attempted |

Predicted totals: devscout 4 correct / 4 partial, rg 6 / 2, ast-grep 2 / 2 attempted, all as the
0.7.0 run. Predicted band verdicts, unchanged: locate a tie on correctness with rg faster;
references to rg and ast-grep; impact to devscout on precision and tool calls; e2e-retrieval to
rg. rg leads warm wall time in all four bands; setup never breaks even. The index build and its
on-disk bytes grow (the graph gains the `bus-hop` edges and the fragments carry publish-site
facts); by how much is not predicted.

Every devscout cell whose output differs from the 0.7.0 run's stream is rerun once with
`--no-bus`; that rerun is predicted byte-identical to the 0.7.0 stream, which attributes the whole
difference to bus traversal.

### Scorecard grading

The 0.7.0 run's registered rules, unchanged, the references rule included: core set = the files
naming the symbol as a whole word minus its declaring file (R1 18 files, R2 8); `correct` at ≥75%
of core with ≤2 files outside core ∪ admissible, `partial` at ≥40%; impact graded set = the
seed's hop-1 dependents (I1 the 8 files of R2's core, I2 the 5 files naming `IntervalRetryPolicy`
beyond its declaring file), `correct` needs every graded file within 5x the graded set, `partial`
at least half; locate and e2e-retrieval `correct` name the primary file. One addition, fixed here
before any output exists: an output byte-identical to the 0.7.0 run's stream for the same cell
carries that cell's grade (this is what carries E1's adjudicated `partial`). Before any 0.8.0 cell
is graded, the grader is run over the 0.7.0 run's stored streams and must reproduce every grade,
recall and precision that run published.

## 0.8.0 release run

Measured 2026-09-25 on the release commit, offline throughout: fresh corpus copies, fresh
indexes, an oracle regenerated from the release tree, and a compiler-facts artifact produced and
admitted in this run rather than reused.

### Pin

```
Corpus            MassTransit/MassTransit @ 855cf1752c94ca9498e0c45ce8d09fdc9e957dd6
                  `git archive` export per arm, HEAD at the pin, tree clean, no remote, no .git/scout
Source            release commit 58a9811db1eb52f7e96d0d851ca40783befcd7cf (`devscout --version`: devscout 0.8.0)
Binary build      `git archive` export of that commit, `cargo build --release --locked -j 4 --offline`
                  (48.9 s), isolated HOME/TMPDIR/CARGO_TARGET_DIR, rustc 1.97.1 (8bab26f4f 2026-07-14),
                  aarch64-apple-darwin; pinned by commit, toolchain and command, since the binary's
                  sha256 depends on the build directory
Comparison binary devscout 0.7.0 from ed6fc11f3b11e7dbc2a6b94c38b83a3860d777e7, built the same way
Oracle build      same export, `dotnet restore tools/scout-semantic --locked-mode`,
                  `dotnet build tools/scout-semantic --no-restore -c Release`, SDK 9.0.305 (MSBuild 17.14)
Oracle run        `dotnet restore MassTransit.sln -p:TargetFrameworks=net9.0`, then
                  `dotnet run --no-restore --project tools/scout-semantic -c Release` with
                  `-p:TargetFrameworks=net9.0` (40.5 s); 56 units ok, 0 failed; the pre-walk restore
                  step restored no project
refs.jsonl        sha256 f9b579f0629a0b930f37723edab102d0a185fc07971582988cc4272e82f5c3a6
                  112190 records / 66924 sites
units.jsonl       sha256 3b7137cfff70996059ae07cadd6ff55e315f9fdaa29ef01ce1c1f0a4bc2c8fc6
graph.json        default lane: sha256 b93707a2c200f563987101d12208d5d601cfbe29c1750c006561186e56a8d7ce
                  5634 files, 9951 defs, 159605 edges (135918 non-bus + 23687 bus-hop); byte-identical
                  on every copy 0.8.0 mapped without an admitted artifact (audit, oracle, scorecard,
                  enriched-lane copy before admission) and after `map --no-semantic` on the enriched
                  copy; fragments-v23.json sha256
                  9204bf931ee7157cab99b65e674a84264b1c5c93b1a598e9bc7248f2a18ae927 on every copy
                  0.7.0 binary, same audit copy: sha256 f6523adde51480cf4fb9e051f529e7ab741c353063e3db1e59b018b6ee8da999
                  enriched lane: sha256 58dc0d78c4d004e071f26a54d97029abe62d19da016d265874b4fbf20f56e55a,
                  223492 edges (23687 bus-hop)
Precise wrong-target `--fp-sites` rows, sorted, sha256
                  f9698caf6431061ff7bf5190cc9ccf7fd64f7cfde2241dfe7794042122dceb5b (170 rows); the whole
                  4158-row `--fp-sites` file is byte-identical to the 0.7.0 run's
Compiler facts    `--emit compiler-facts --tfm net9.0 -p Configuration=Debug -p Platform=AnyCPU
                  -p:TargetFrameworks=net9.0 -p:NuGetAudit=false`, 284,586,840 bytes, sha256
                  7f9120a5aaa6d22c0fe2774ee6522c01e3f8f4ec6d4e6fb3bdc9d027672e5f1e (see "Offline
                  conditions" for why the artifact is not byte-reproducible)
```

### Offline conditions and deviations

1. **Every restore ran with no network.** NuGet's vulnerability audit cannot reach its feed, so
   by default each project restore records `NU1900` ("Error occurred while getting package
   vulnerability data"), as in the 0.7.0 run. The oracle's output does not depend on it: the
   oracle walk over a restore with the audit on (56 of its 62 workspace diagnostics are `NU1900`)
   and over a restore with `-p:NuGetAudit=false` (6 workspace diagnostics) produce the same
   `refs.jsonl` and `units.jsonl`, byte for byte.
2. **The compiler-facts run does depend on it.** The workspace reports each `NU1900` as a failure
   naming its project; the compiler-facts producer marks a compilation with such a failure
   `partial` / `workspace-failure`, and the consumer confirms nothing from a compilation that is
   not `complete`. The first enriched pass, on the audit-on restore, therefore admitted an
   artifact in which every one of the 54 loaded compilations was `partial` (53
   `workspace-failure`, 1 `binding-error`): `map`
   recorded `stats.semantic` as 0 confirmed, 0 disagreements, 0 discovered, and `audit` reported
   `lane enriched` with every tier and figure equal to the default lane's. Nothing in `map`'s or
   `audit`'s output says why. The enriched-lane figures below come from a second pass on a fresh
   corpus copy restored with `-p:NuGetAudit=false` (passed to the restore, the oracle and the
   compiler-facts run alike), where 53 compilations are `complete`, 2 `unsupported`
   (`undeclared-target`) and 1 `partial` (`binding-error`, the incomplete unit Runs 7 and 8 also
   report). The default-lane and scorecard figures do not depend on this choice.
3. `dotnet run` took `--no-restore` so it could not reach for a feed; the oracle tool itself was
   restored in locked mode from the local package cache.
4. **The compiler-facts artifact is not byte-reproducible run to run.** Five runs on two copies
   restored the same way (the enriched pass, the pre-fix producer check below, and the three
   timing runs) differ only in the order of the compiler diagnostics recorded for the one
   incomplete unit (`MassTransit.Analyzers.Tests`), and so in that unit's context fingerprint.
   Symbols and occurrences are otherwise identical, and admission reports the same counts.
5. The host was shared. Load averages read 8.1 / 11.7 / 8.8 (1, 5, 15 min) before the scorecard
   timing pass and 12.0 / 12.3 / 9.2 after it; compare wall times only between arms of this run.

### Default lane: audit text output (verbatim; `root` rewritten bench-relative, nothing else changed)

```
devscout audit --semantic  root bench/corpora/csharp  lane syntax  oracle 112190 records / 66924 sites  units ok 56 failed 0  method units
tier        edges     tp     fp   precision   fp:no-site  fp:external  fp:wrong  structural  unjudged
precise     31240  31026    214       0.993           44            0       170           0         0
ext          2543   2521     22       0.991            5           17         0           0         0
guess        9007   5085   3922       0.565           48         2064      1810           0         0
recall (57607 in-graph member sites)  precise 0.528  precise+ext 0.571  all 0.659
  by receiver  ident 0.759  qualified 0.288  this 0.944  base 0.978  call 0.062
external sites 19153  silent-correct 18222  leaked 931
structural  impossible 2  checked 42786
fan-out  1: 26516  2: 4949  3: 1483  4+: 442
top fp targets   InMemoryDelayProvider 295  RoutingSlipExtensions 161  BusRegistrationContext 148  IBusRegistrationContext 148  IPerformanceCounter 148  NullPerformanceCounter 148  StatsDPerformanceCounter 148  Retry 124  InMemoryContainerTestFixture 83  ContainerTestHarness 78  TextTableOptions 65  IRegistrationConfigurator 53  IIndexedSagaProperty 50  IndexedSagaDictionary 50  IndexedSagaProperty 50  SuperComplexRequest 44  ConsumerPipeConfiguratorExtensions 42  RetryConfigurationExtensions 42  SagaPipeConfiguratorExtensions 42  ResponseHandlerConnectHandle 40
top missed       MassTransit.ConsumeContext 1021  MassTransit.BehaviorContext 674  MassTransit.Testing.IBaseTestHarness 621  MassTransit.Testing.ITestHarness 600  MassTransit.TransitionExtensions 449  MassTransit.SendContext 373  MassTransit.IStateMachineModifier 340  MassTransit.ThenExtensions 319  MassTransit.DependencyInjectionTestingExtensions 294  MassTransit.Testing.IReceivedMessageList 278  MassTransit.Testing.IPublishedMessageList 272  MassTransit.TestStateMachineExtensions 268  MassTransit.IRegistrationConfigurator 262  MassTransit.SagaConsumeContext 210  MassTransit.IPublishEndpoint 186  MassTransit.ISendEndpoint 178  MassTransit.Testing.AsyncElementListExtensions 170  MassTransit.IBusControl 161  MassTransit.ConsumerExtensions 158  MassTransit.Headers 145
unknown targets  class 3  record 1  struct 1
partial file mismatch 155
ambiguous 137
edges outside universe (not judged) 733
```

### Default lane: `--json` tier objects and lane (verbatim)

```json
{
  "lane": "syntax",
  "tiers": {
    "precise": {
      "edges": 31240,
      "tp": 31026,
      "fp": 214,
      "precision": 0.993,
      "fp_no_site": 44,
      "fp_external_site": 0,
      "fp_wrong_target": 170,
      "structural": 0,
      "unjudged": 0
    },
    "ext": {
      "edges": 2543,
      "tp": 2521,
      "fp": 22,
      "precision": 0.991,
      "fp_no_site": 5,
      "fp_external_site": 17,
      "fp_wrong_target": 0,
      "structural": 0,
      "unjudged": 0
    },
    "guess": {
      "edges": 9007,
      "tp": 5085,
      "fp": 3922,
      "precision": 0.565,
      "fp_no_site": 48,
      "fp_external_site": 2064,
      "fp_wrong_target": 1810,
      "structural": 0,
      "unjudged": 0
    }
  }
}
```

Recall by receiver kind, the external-receiver leak, fan-out, the top-20 false-positive and
missed-target lists and the unknown-target counts are all identical to the 0.7.0 run's tables,
figure for figure, so they are not repeated here. Apart from the `lane` field and the `unjudged`
column, the text output is the 0.7.0 run's line for line, and the `--json` output is that run's
once the `lane` and `unjudged` keys are set aside.

### Default lane: predictions vs. actual

| # | Predicted | Actual | Verdict |
| --- | --- | --- | --- |
| P1 | oracle digests `f9b579f0…c3a6` / `3b7137cf…fc6`; 56 ok, 0 failed; no project restored | identical; the restore step restored nothing | **HOLD** |
| P2 | `graph.json` gains the bus edges and two stats entries; without them it equals the 0.7.0 binary's graph, itself `f6523add…a999` | 0.7.0 graph `f6523add…a999`; the 0.8.0 graph without its 23687 `bus-hop` edges, their counter and `bus_vocabulary_derived` has the same top-level keys, the same 135918 edges in the same order and the same stats | **HOLD**, with two corrections to the wording: the counter lives at `stats.edges_by_kind["bus-hop"]`, not `stats["bus-hop"]`; and the bus edges follow every other C# edge but precede the corpus's four TypeScript edges (one `import`, three `call`, from its documentation site), which close the list in both versions |
| P3 | tier objects as the 0.7.0 run, `unjudged` 0, `lane` `syntax` | identical | **HOLD** |
| P4 | recall 0.528 / 0.571 / 0.659; by receiver 0.759 / 0.288 / 0.944 / 0.978 / 0.062 | identical | **HOLD** |
| P5 | 19153 / 18222 / 931; 2 / 42786 | identical | **HOLD** |
| P6 | fan-out 26516 / 4949 / 1483 / 442; 155 / 137 / 733; unknown 3 / 1 / 1 | identical | **HOLD** |
| P7 | top-20 lists as the 0.7.0 run; 170 rows `f9698caf…5b` | identical, and the whole `--fp-sites` file is byte-identical | **HOLD** |

All seven hold. The default lane scores exactly as 0.7.0 does; the `bus-hop` edges are its only
graph change on this corpus.

### Bus-hop reach

Read from the default lane's `graph.json`. These are candidate type relationships between a
publish site and a handler declaration; how many of them are real routes is not measured here.

| Figure | Value |
| --- | --- |
| `bus-hop` edges (`stats.edges_by_kind["bus-hop"]`) | 23687 |
| distinct messages (array suffix included) | 171 |
| publish sites / publisher files | 651 / 237 |
| handler declarations / handler files | 276 / 176 |
| hops whose publisher and handler sit in different files | 23144 |
| `stats.bus_vocabulary_derived` | `true` |
| distinct handlers per message: median / mean / max | 1 / 1.71 / 79 |
| messages by handler count | 1: 138, 2: 26, 3: 3, 4: 1, 5: 1, 6: 1, 79: 1 |

| Evidence word | Hops | Share | Messages |
| --- | --- | --- | --- |
| `base-arg` | 22474 | 0.949 | 163 |
| `nested-base-arg` | 1199 | 0.051 | 12 |
| `mediator-request` | 9 | 0.0004 | 4 |
| `property-arg` | 5 | 0.0002 | 1 |

| Message | Hops | Share | Handlers | Publish sites |
| --- | --- | --- | --- | --- |
| `MassTransit.TestFramework.Messages.PingMessage` | 23068 | 0.974 | 79 | 292 |
| `MassTransit.Tests.Saga.Messages.InitiateSimpleSaga` | 116 | 0.005 | 4 | 29 |
| `MassTransit.Tests.ContainerTests.Scenarios.SimpleMessageInterface` | 66 | 0.003 | 6 | 11 |
| `MassTransit.TestFramework.Messages.PongMessage` | 28 | 0.001 | 2 | 14 |
| `MassTransit.TestFramework.Messages.PingMessage[]` | 4 | 0.0002 | 2 | 2 |

The other 166 messages together carry 405 hops. One shared test message, published from 292
sites to 79 handlers, is 97.4% of all hops; every hop row states that message's handler count,
which is how a reader tells this contract apart from a single route.

**`--no-bus` against 0.7.0.** All 18 registered answers (five `refs` seeds and four `impact`
seeds, text and `--json` each) from 0.8.0 with `--no-bus` are byte-identical to the 0.7.0
binary's answers on the same corpus copy. Both binaries answer `refs PingMessage` with the same
two-candidate ambiguity (the test-framework class and a test-local record), exit 1.

**What bus traversal changes on the same seeds (reach, not predicted).** The five `refs` answers
and the `IJobService.cs` and `IntervalRetryPolicy.cs` impact answers are byte-identical with and
without `--no-bus`. The two message files' impact answers change through the file in-degree
brake rather than through new rows: bus edges count as referring files, so the handler test
files cross the brake and stop expanding.

| `impact --hops 2` seed | affected files, `--no-bus` / default | files braked, `--no-bus` / default | bus-only rows among the 50 shown |
| --- | --- | --- | --- |
| `src/MassTransit.TestFramework/Messages/PingMessage.cs` | 809 / 759 | 2 / 57 | 0 |
| `src/MassTransit.TestFramework/Messages/PongMessage.cs` | 622 / 604 | 2 / 19 | 0 |

### Bus-hop reach: predictions vs. actual

| # | Predicted | Actual | Verdict |
| --- | --- | --- | --- |
| B1 | 23687 edges and counter | 23687 both (counter at `stats.edges_by_kind["bus-hop"]`) | **HOLD** |
| B2 | 171 messages | 171 | **HOLD** |
| B3 | 23068 hops, share 0.974, 79 handlers | 23068, 0.974, 79 | **HOLD** |
| B4 | array: 4 hops, 2 sites, 2 handlers | 4, 2, 2 | **HOLD** |
| B5 | max 79 / median 1 | 79 / 1 | **HOLD** |
| B6 | `base-arg` at least 95%; each other word under 5% | `base-arg` 94.9%, `nested-base-arg` 5.1% | **MISS** — see below |
| B7 | `bus_vocabulary_derived` `true` | `true` | **HOLD** |
| B8 | `--no-bus` answers byte-identical to 0.7.0 | 18 of 18 byte-identical | **HOLD** |

**B6, explained.** The shared test message's hops are not all `base-arg`: 1168 of its 23068 hops
reach handlers that name it as the inner argument of a single-argument generic wrapper
(`nested-base-arg`), and the other 170 messages add 31 more. The prediction assumed every one of
that message's 79 handlers declared it directly on its base list. No count or reach figure moved;
only the split between two evidence words was unknown in advance.

### Enriched lane (opt-in)

```
Corpus copy       fresh export at the pin, restored with -p:TargetFrameworks=net9.0 -p:NuGetAudit=false
Oracle            re-walked on that copy: refs.jsonl / units.jsonl byte-identical to the default lane's
Compiler-facts    `dotnet run --no-restore --project tools/scout-semantic -c Release -- MassTransit.sln
run               --root <copy> --emit compiler-facts --compiler-facts <file> --tfm net9.0
                  -p Configuration=Debug -p Platform=AnyCPU -p:TargetFrameworks=net9.0 -p:NuGetAudit=false`
                  43.9 s wall; 34161 symbols, 21 diagnostics, 1 incomplete unit
                  (MassTransit.Analyzers.Tests|net9.0); compilations 53 complete, 2 unsupported, 1 partial
Admission         `devscout compiler-facts import`: admitted (engine 2), coverage incomplete (1 unit),
                  163606 occurrences admitted, 18 ambiguous, 1812 unresolved; 3.5 s
Rebuild           `devscout map .`: 9951 defs, 223492 edges; `stats.semantic` 53251 confirmed,
                  1470 disagreements, 49684 discovered; 6.3 s
Syntax pairing    the same copy mapped before admission, and `map --no-semantic` after it, both give
                  the default lane's graph.json byte for byte
```

**Consumer conformance only**, as for Runs 7 and 8: both lanes are scored against the same
oracle output, not against independently verified truth.

#### Enriched-lane audit text output (verbatim; `root` rewritten bench-relative, nothing else changed)

```
devscout audit --semantic  root bench/corpora/csharp  lane enriched  oracle 112190 records / 66924 sites  units ok 56 failed 0  method units
tier        edges     tp     fp   precision   fp:no-site  fp:external  fp:wrong  structural  unjudged
precise      1335   1281     54       0.960           44            0        10           0         0
ext            89     67     22       0.753            5           17         0           0         0
guess        2318    132   2186       0.057           48         2060        78           0         0
semantic    53251  53162     89       0.998            0           85         4           0         0
semantic-discovered  49684   8803   2715       0.764         1747          149       819           2     38166
recall (57607 in-graph member sites)  precise 0.920  precise+ext 0.921  all 0.945
  by receiver  ident 0.992  qualified 0.912  this 1.000  base 1.000  call 0.317
external sites 19153  silent-correct 12356  leaked 6797
structural  impossible 86  checked 106677
fan-out  1: 39780  2: 17331  3: 5494  4+: 3525
top fp targets   TransitionExtensions 442  ThenExtensions 368  InMemoryDelayProvider 295  DependencyInjectionTestingExtensions 292  IStateMachineModifier 279  RoutingSlipExtensions 161  IPerformanceCounter 148  NullPerformanceCounter 148  StatsDPerformanceCounter 148  IStateMachineEventActivitiesBuilder 98  InMemoryContainerTestFixture 83  PublishExtensions 66  TextTableOptions 65  IConsumerRegistrationConfigurator 61  JsonConverterFactoryExtensions 60  EventActivityBinder 57  MassTransitStateMachine 57  DependencyInjectionRegistrationExtensions 56  IIndexedSagaProperty 50  IndexedSagaDictionary 50
top missed       MassTransit.TransitionExtensions 435  MassTransit.ThenExtensions 304  MassTransit.DependencyInjectionTestingExtensions 291  MassTransit.IStateMachineModifier 274  MassTransit.IStateMachineEventActivitiesBuilder 98  MassTransit.ConsumeContext 94  MassTransit.PublishExtensions 66  MassTransit.IConsumerRegistrationConfigurator 61  MassTransit.Serialization.JsonConverters.JsonConverterFactoryExtensions 60  MassTransit.EventActivityBinder 57  MassTransit.DependencyInjectionRegistrationExtensions 55  MassTransit.Fault 55  MassTransit.JobSaga 55  MassTransit.ScheduleTimeSpanExtensions 46  MassTransit.RespondExtensions 37  MassTransit.StateMachineRequestExtensions 37  MassTransit.KafkaDependencyInjectionTestingExtensions 33  MassTransit.JobStateMachineBehaviorExtensions 30  MassTransit.IServiceBusNamedKeyTokenProviderConfigurator 23  MassTransit.HostInfo 19
unknown targets  class 3  record 1  struct 1
partial file mismatch 1791
ambiguous 137
edges outside universe (not judged) 733
```

#### What moved against Run 8, and why

Run 8's figures cannot be compared with these directly, because the two runs did not score the
same compiler-facts input. To separate the input from the code, the devscout binary Run 8's
environment names (`6a47cc7e96042f4610857901fb90f26a2606da26`, built the same way) was run on a
fresh map of the same copy with **this run's** admitted artifact, against the same oracle output:

| Tier (edges / tp / fp) | Run 8 as published | Run 8's binary, this artifact | 0.8.0, this artifact |
| --- | --- | --- | --- |
| `precise` | 3982 / 3859 / 123 | 1364 / 1282 / 82 (`fp:external` 28) | 1335 / 1281 / 54 (`fp:external` 0) |
| `ext` | 387 / 365 / 22 | 89 / 67 / 22 | 89 / 67 / 22 |
| `guess` | 3567 / 1043 / 2524 | 2343 / 132 / 2211 (`fp:external` 2097, `fp:wrong` 66) | 2318 / 132 / 2186 (`fp:external` 2060, `fp:wrong` 78) |
| `semantic` | 46445 / 46357 / 88 | 53251 / 53162 / 89 | 53251 / 53162 / 89 |
| `semantic-discovered` | 46082 / 8075 / 2406, `unjudged` 35601 | 49668 / 8787 / 2715, `unjudged` 38166 | 49684 / 8803 / 2715, `unjudged` 38166 |
| recall precise / precise+ext / all | 0.848 / 0.854 / 0.891 | 0.920 / 0.921 / 0.945 | 0.920 / 0.921 / 0.945 |

Two separate differences, each confined to one pair of columns:

- **Run 8 → Run 8's binary on this artifact: the input.** The consumer is the same, yet the
  `semantic` row grows by 6806 edges, `semantic-discovered` by 3586, and all three recall figures
  rise: this artifact confirms more. 53 of its 54 loaded compilations are `complete`. Run 8's own
  artifact was not kept, so which of its compilations were not `complete` cannot be read back.
  What is on record: Run 8 reused a corpus clone restored with network access during Run 6's
  setup, and its oracle walk logged 26 workspace diagnostics, against 6 here; a workspace failure naming a project demotes that compilation, and
  the first enriched pass above shows that mechanism removing every confirmed fact when all
  projects carry one. The likely explanation, **not proven**, is that that networked restore
  recorded package-audit warnings on some projects and so demoted their compilations. One other
  candidate, the oracle packaging fix, is ruled out: the producer from the revision before that
  fix, run on the same restored copy, yields the same compilation states and the same symbols,
  and its occurrences differ only in the incomplete unit's fingerprint.
- **Run 8's binary → 0.8.0 on the same artifact: the code.** Exactly the two predicted changes.
  The precise-tier refusals remove the residual precise row's 28 `fp:external` edges (and one
  true positive), and guess `fp:external` falls by 37 while guess `fp:wrong` rises by 12, the same
  moves the syntax lane made from Run 6 to 0.7.0. The eight `MessageFactory.Create` sites add 16
  `semantic-discovered` edges, all true positives. `semantic`, `ext` and every recall figure are
  unchanged.

#### Registered shipping gate, at this tree

Against this tree's own syntax lane (precise 31026 / 31240 = 0.99315, precise+ext 0.571):

| # | Criterion | Syntax lane | Enriched lane | Result |
| --- | --- | --- | --- | --- |
| G-1 | recall precise+ext gain at least +0.10 absolute and at least 0.70 | 0.571 | 0.921 (+0.350) | **met** |
| G-2 | union of `precise` and `semantic` not more than 0.005 below the syntax lane's precise figure | 0.99315 | 54443 / 54586 = 0.99738 (+0.00423) | **met** |
| G-3 | cold acquisition at most 300 s wall | n/a | 43.9 s | **met** |
| G-4 | cold acquisition at most 2x `dotnet build -c Release` of the same solution | n/a | acquisition 40.96 / 41.46 / 41.59 s against Release build 14.69 / 16.98 / 14.80 s: 2.41x-2.83x (2.80x median to median), against the 53 projects that build | **not demonstrated** |

G-4 was measured on a corpus copy of its own, restored the same way, with the build server shut
down before every run. The acquisition was timed as the engine run directly
(`dotnet tools/scout-semantic/bin/Release/net9.0/scout-semantic.dll`, same arguments as above),
three times. The Release build, `dotnet build MassTransit.sln -c Release --no-restore
-p:TargetFrameworks=net9.0 -p:NuGetAudit=false`, ran three times with every `bin/Release` and
`obj/Release` removed first. Each build exits 1: 53 projects produce output and two stop at
`NETSDK1005` under the `net9.0` override (`MassTransit.Analyzers`, whose assets file has no
`netstandard2.0` target, and `MassTransit.MartenIntegration.Tests`, likewise for `net8.0`), as in
Run 8. The ratio is therefore against a subset, and the criterion is not demonstrated; against
that subset it would not be met either.

#### Enriched lane: predictions vs. actual

| # | Predicted | Actual | Verdict |
| --- | --- | --- | --- |
| N1 | compiler-facts run as Run 8 | 34161 / 21 / 1 incomplete; 163606 admitted (161776 confirmed, 1812 unresolved, 18 ambiguous) | **HOLD** |
| N2 | admitted, incomplete (1 unit); `lane` `enriched` | as predicted | **HOLD** |
| N3 | `semantic` 46445 / 46357 / 88 | 53251 / 53162 / 89 | **MISS** — the input, above |
| N4 | `semantic-discovered` 46098 / 8091, `unjudged` 35601 | 49684 / 8803, `unjudged` 38166 | **MISS** on the base (the input); the predicted +16 true-positive edges hold exactly against Run 8's binary on the same artifact |
| N5 | residual precise: `fp:external` 0, `fp:no-site` 44, `fp:wrong` ≤ 51, `structural` 0, fp ≤ 95, precision ≥ 0.975, edges fall | 0 / 44 / 10 / 0, fp 54, precision 0.960, 1335 edges | **MISS** on precision only: the residual population is 1335 edges rather than about 3950, so its unchanged 44 `fp:no-site` weigh three times as much; every class count holds |
| N6 | residual ext/guess move as the syntax lane did; guess `fp:external` falls | guess `fp:external` 2097 → 2060 on the same artifact; `ext` unchanged | **HOLD** |
| N7 | recall 0.848 / 0.854 / 0.891, each within 0.001 | 0.920 / 0.921 / 0.945 | **MISS** — the input, above; identical under Run 8's binary on this artifact |
| N8 | 23687 `bus-hop` edges, identical to the default lane's | identical, edge for edge | **HOLD** |
| N9 | `map --no-semantic` gives the default lane's `graph.json` | byte-identical | **HOLD** |
| G-1 | met, 0.854 (+0.283) | met, 0.921 (+0.350) | **HOLD** on the outcome; figure as N7 |
| G-2 | met, about 0.996 | met, 0.99738 | **HOLD** |
| G-3 | met | met, 43.9 s | **HOLD** |
| G-4 | not demonstrated, the same two projects fail | not demonstrated, the same two | **HOLD** |

Every prediction about the code holds; each miss comes from one fact the predictions did not
have: this run's artifact confirms more than Run 8's did. The gate outcome is Run 8's: criteria
1-3 met, criterion 4 not demonstrated.

## Scripted scorecard, devscout 0.8.0

Same eight tasks, arms, commands and grading as the 0.7.0 run; only the devscout binary and the
corpus copy changed.

```
Date            2026-09-25
Corpus          MassTransit/MassTransit @ 855cf1752c94ca9498e0c45ce8d09fdc9e957dd6 — `git archive`
                export at bench/corpora/csharp, HEAD at the pin; 5634 files indexed
Tool version    devscout 0.8.0, release commit 58a9811db1eb52f7e96d0d851ca40783befcd7cf
Build           cargo build --release --locked -j 4 --offline, rustc 1.97.1 (8bab26f4f 2026-07-14),
                aarch64-apple-darwin
Host            macOS (Darwin 25.2.0), Apple M2 Max, arm64; 12 cores, 64 GiB RAM — shared, see
                "Offline conditions" item 5
Baseline        ripgrep 15.2.0 (+PCRE2 10.45);  peer run: ast-grep 0.45.1
Bench root      bench/ (throwaway); verbatim streams stored as raw bytes
Isolation       HOME, TMPDIR, SCOUT_REGISTRY and SCOUT_CONTENT_DB redirected under the bench root
Network         none, setup included
Reps            cold 1 + warm median of 5 per cell, in-process timer around each subprocess
Grader check    run over the 0.7.0 run's stored streams first: every grade, recall, precision,
                file count and reach verdict that run published came out identical
```

| Arm | Index build | On-disk bytes | Break-even |
| --- | --- | --- | --- |
| devscout | 3.178 s, median of 5 full cold rebuilds (min 3.157, max 3.752); 0.7.0 run: 3.020 s | 118,751,395 = graph 55,336,847 + fragments 61,378,511 + fragments-index 538,994 + project units 16,656 + manifest 1,480,217 + state 170; 0.7.0 run: 108,505,951 | never here |
| rg / ast-grep | none | 0 | n/a |

Never, for the same reason as before: devscout trails on both per-task axes, mean warm 247.7 ms
against rg's 113.1 ms and mean payload 2058 against 1572 tokens.

### Every cell

`D` = `devscout -C bench/corpora/csharp`, `C` = `bench/corpora/csharp`; commands as 2026-08. Grades
read `grade recall/precision, bytes`.

| id | kind | devscout 0.8.0 | devscout 0.7.0 run | rg | ast-grep |
| --- | --- | --- | --- | --- | --- |
| L1 | locate | correct, 2692 B, reach yes | same bytes | correct, 3340 B, reach yes | not attempted |
| L2 | locate | correct, 1393 B, reach yes | same bytes | correct, 623 B, reach yes | not attempted |
| R1 | references | correct 1.000/0.947, 19 files, 10172 B under `--all`; plain form partial 0.722/0.929, 14 files, 5618 B | same bytes | correct 1.000/0.947, 9827 B | correct 1.000/0.947, 16574 B |
| R2 | references | partial 1.000/0.667, 12 files, 5189 B | same bytes | correct 1.000/0.889, 1859 B | correct 1.000/0.889, 3065 B |
| I1 | impact | partial 1.000/0.157, 51 rows over a 40-row cap, 15719 B | same bytes | partial 1.000/0.051, 156 files, 16294 B, 4 calls | not attempted |
| I2 | impact | partial 1.000/0.098, 51 rows over a 25-row cap, 15959 B | same bytes | partial 1.000/0.060, 84 files, 9561 B, 4 calls | not attempted |
| E1 | e2e-retrieval | partial, 36 files, 8341 B, reach yes | same bytes | correct, 7368 B | not attempted |
| E2 | e2e-retrieval | correct, 6392 B, reach yes | same bytes | correct, 1431 B | not attempted |

**devscout 4 correct / 4 partial, rg 6 / 2, ast-grep 2 / 2 attempted, all as in the 0.7.0 run.**
Every devscout stream, the plain R1 form included, is byte-identical to the 0.7.0 run's, so no
cell needed a `--no-bus` rerun. R2 stays partial under the unchanged registered rule: its three
extra files are real calls of `IJobService` members through a property typed with it, which a
truth set built by text search cannot hold. E1 carries the 0.7.0 run's adjudicated `partial`
under the byte-identity rule registered above. The rg and ast-grep streams have the 0.7.0 run's
bytes and lines; nine of them list those lines in a different order, because both tools search
files in parallel.

### Correctness, cost, wall time, reach

| Arm | correct | partial | wrong | not attempted | correct+partial |
| --- | --- | --- | --- | --- | --- |
| devscout | 4 | 4 | 0 | 0 | 8 / 8 |
| rg | 6 | 2 | 0 | 0 | 8 / 8 |
| ast-grep | 2 | 0 | 0 | 6 — locate, impact, e2e-retrieval; "no symbol table or cross-file graph" | 2 / 2 attempted |

Median of per-task ratios (rg bytes / devscout bytes) **0.74**; ratio of pooled medians (5354 /
7366.5) **0.73**; both as in the 0.7.0 run.

| Band | devscout cold / warm ms | rg cold / warm ms | reach: devscout | reach: rg |
| --- | --- | --- | --- | --- |
| locate | 189 / 185 | 119 / 86 | yes | yes |
| references | 455 / 313 | 165 / 88 | yes | yes |
| impact | 329 / 303 | 259 / 188 (4 calls) | no | no |
| e2e-retrieval | 193 / 189 | 101 / 90 | yes | no |

Band figures are the mean of the band's two tasks. The references band's devscout cold figure
carries one outlier (R2 cold 583.9 ms, warm 328.8 ms). devscout's mean warm time is 247.7 ms
against 215.7 ms in the 0.7.0 run, while rg's fell from 131.7 to 113.1 ms. The devscout increase
follows the graph file, which grows from 47.1 MB to 55.3 MB with the `bus-hop` edges and is read
on every query. Timed side by side (seven interleaved runs each of `refs MessageUrn --json` on
this host, same sitting), 0.7.0 on its own graph took a median 269.7 ms, 0.8.0 298.8 ms with
`--no-bus` and 299.9 ms without: the difference is the larger graph, not bus traversal.

### Where peers win

Derived from the grid by the harness, one line per band, the arm that leads each metric:

- **locate** — correctness tie (devscout, rg); payload bytes rg; warm wall time rg; tool calls tie;
  reach tie.
- **references** — correctness rg and ast-grep; precision rg and ast-grep; payload bytes rg; warm
  wall time rg; tool calls tie; reach tie.
- **impact** — correctness tie (both partial); precision devscout (0.157 / 0.098 against 0.051 /
  0.060); tool calls devscout (1 against 4); payload bytes rg; warm wall time rg; reach tie (neither).
- **e2e-retrieval** — correctness rg; payload bytes rg; warm wall time rg; reach devscout; tool calls
  tie.

rg leads warm wall time in all four bands, payload bytes in all four, and correctness in
`references` and `e2e-retrieval`. devscout leads only `impact` precision and tool calls, and
`e2e-retrieval` reach. ast-grep matches rg's reference file sets and grades at 4.5x rg's warm time
on `MessageUrn` (398.6 ms against 89.2 ms).

### Scorecard predictions vs. actual

| Registered | Actual | Verdict |
| --- | --- | --- |
| L1, L2, R1, R2, E1, E2 devscout byte-identical to the 0.7.0 run, grades carried | byte-identical, grades as predicted | **HOLD** |
| I1, I2 devscout partial, recall 1.0, over the cap, precision above rg's; bytes not predicted | as predicted, and byte-identical to the 0.7.0 run | **HOLD** |
| rg and ast-grep cells byte-identical to the 0.7.0 run | same bytes, lines and grades; nine streams list their lines in a different order | **MISS on literal byte identity**, by output order only — parallel search |
| totals devscout 4 / 4, rg 6 / 2, ast-grep 2 / 2 | as predicted | **HOLD** |
| band verdicts as the 0.7.0 run; rg leads warm time in all four bands; setup never breaks even | as predicted | **HOLD** |
| a changed devscout cell reproduces 0.7.0 under `--no-bus` | no cell changed | not exercised |

## Before and after

| Metric | 0.7.0 release run | 0.8.0 release run |
| --- | --- | --- |
| default-lane precise edges / precision | 31240 / 0.993 | 31240 / 0.993 |
| default-lane precise `fp:no-site` / `fp:external` / `fp:wrong` / `structural` | 44 / 0 / 170 / 0 | 44 / 0 / 170 / 0 |
| default-lane recall precise / precise+ext / all | 0.528 / 0.571 / 0.659 | 0.528 / 0.571 / 0.659 |
| default-lane graph edges | 135918 | 159605 (+23687 `bus-hop`) |
| enriched-lane recall precise / precise+ext / all | not measured at that tree (Run 8, an earlier revision and input: 0.848 / 0.854 / 0.891) | 0.920 / 0.921 / 0.945 |
| enriched-lane `precise` ∪ `semantic` precision | not measured at that tree (Run 8: 0.99582) | 0.99738 |
| scorecard devscout / rg / ast-grep | 4/4, 6/2, 2/2 | 4/4, 6/2, 2/2 |
| index build, on-disk bytes | 3.020 s, 108.5 MB | 3.178 s, 118.8 MB |
| devscout mean warm query | 215.7 ms | 247.7 ms (the larger graph file; see above) |

Worse: every devscout query reads a graph file 17% larger, and the index takes about 5% longer
to build and 9% more disk, all from the `bus-hop` edges. The enriched lane reports
`lane enriched` while confirming nothing when every compilation it was given is `partial`, which
is what an offline restore with the package audit left on produces, and nothing in its output
says so. Better: nothing in the default lane's scoring moved, `--no-bus` answers exactly as 0.7.0
did, and the opt-in enriched lane, on an artifact whose compilations are complete, confirms 53251
references and reaches 0.921 precise+ext recall at 0.99738 precision for the precise and
semantic tiers together, against the same oracle it was produced from.
